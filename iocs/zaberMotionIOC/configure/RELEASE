# RELEASE - Location of external support modules
EPICS_BASE=/Users/colby.sparks/EPICS/base-7.0.8.1
EPICS_SUPPORT=$(EPICS_BASE)/../support
MOTOR=$(EPICS_SUPPORT)/motor
ASYN=$(EPICS_SUPPORT)/asyn
BUSY=$(EPICS_SUPPORT)/busy
SEQ=$(EPICS_SUPPORT)/sequencer

# Use motor/module's generated release file when buidling inside motor
-include $(TOP)/../../../RELEASE.$(EPICS_HOST_ARCH).local
# Use motorZaberMotion's release file when building inside motorZaberMotion, but outside motor
-include $(TOP)/../../configure/RELEASE.local
# Use zaberMotionIOC's RELEASE.local when building outside motorZaberMotion
-include $(TOP)/configure/RELEASE.local
